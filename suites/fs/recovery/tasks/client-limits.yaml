
overrides:
  zbkc:
    log-whitelist:
      - responding to mclientcaps\(revoke\)
      - not advance its oldest_client_tid

tasks:
  - zbkcfs_test_runner:
      modules:
        - tasks.zbkcfs.test_client_limits
